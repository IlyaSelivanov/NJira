@model NJira.WebUI.Models.TransactionSettingsViewModel

@{
    ViewBag.Title = "Index";
}

<h2>Cart: @ViewBag.Count</h2>

<div class="row">
    <div class="col-sm-4 col-sm-offset-4">

        <h2>Search</h2>

        <div class="panel panel-default">
            <div class="panel-body">
                @using (Html.BeginForm("Transact", "Transaction"))
                {
                    @*<div class="form-group">
                        @Html.LabelFor(m => m.Version)
                        @Html.DropDownListFor(m => m.Version.Name, Model.Versions, null, new { @class = "form-control" })
                    </div>*@

                    <div class="form-group">
                        @Html.LabelFor(m => m.Type)
                        @Html.DropDownListFor(m => m.Type, Model.Types, null, new { @class = "form-control" })
                    </div>

                    <div class="form-group">
                        @Html.LabelFor(m => m.Resolution)
                        @Html.DropDownListFor(m => m.Resolution, Model.Resolutions, null, new { @class = "form-control" })
                    </div>

                    <div class="form-group">
                        @Html.LabelFor(m => m.CodeLocation)
                        @Html.DropDownListFor(m => m.CodeLocation, Model.CodeLocations, new { @class = "form-control" })
                    </div>

                    <div class="form-group">
                        @Html.LabelFor(m => m.Comment)
                        @Html.TextAreaFor(m => m.Comment, new { @class = "form-control" })
                    </div>

                    <button type="submit" class="btn btn-primary">Start</button>
                }
            </div>
        </div>
    </div>
</div>

